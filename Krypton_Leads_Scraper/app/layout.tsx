import './globals.css'
import { Inter } from 'next/font/google'
import { Providers } from './providers'

const inter = Inter({ subsets: ['latin'] })

export const metadata = {
  title: 'Krypton Leads - AI-Powered Local Lead Generation',
  description: 'Generate thousands of high-quality local business leads in seconds. Target restaurants, gyms, salons, and more with our AI-powered Google Maps scraper.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>{children}</Providers>
      </body>
    </html>
  )
}